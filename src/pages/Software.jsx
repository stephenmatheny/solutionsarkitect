import Section from "../components/Section.jsx";

export default function Software() {
  return (
    <Section kicker="Offering" title="Educational Software">
      <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <p className="text-sm text-slate-700">
          We build lightweight tools that streamline school workflows—so educators can focus on students, not paperwork.
          If you have a process that needs to run smoother, let us know and we’ll scope it together.
        </p>
      </div>
    </Section>
  );
}
